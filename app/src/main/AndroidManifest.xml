<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.tesoem.isc.gol.proygol">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".acerca"></activity>
        <activity android:name=".info" />
        <activity android:name=".tipos" />
        <activity android:name=".ataque" />
        <activity android:name=".riesgos" />
        <activity android:name=".objetivos" />
        <activity android:name=".ciberseguridad" />
        <activity android:name=".introduccion" />
        <activity android:name=".menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>